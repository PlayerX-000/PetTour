
/*
    const dadosarquivo = JSON.parse(localStorage.getItem('credentials'));

    console.log(dadosarquivo)
   
if(dadosarquivo[0]!=''||dadosarquivo[0]!=null && dadosarquivo[1]!=''||dadosarquivo[1]!=null){
      document.getElementById("email").value = dadosarquivo[0];
      document.getElementById("senha").value = dadosarquivo[1];
}
    
 
      function alvacache(){
      const email = document.getElementById("email").value;
      const senha = document.getElementById("senha").value;

      let datalogin = [];
      datalogin.push(email,senha)

      localStorage.setItem('credentials',JSON.stringify(datalogin));
    }


*/
      